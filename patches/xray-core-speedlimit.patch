--- /tmp/cursor-sandbox-cache/74593ecae99195a24b357adb44c35647/go-mod/github.com/cedar2025/!xray-core@v0.0.0-20260409213332-f47935539965/app/dispatcher/default.go	2026-08-24 18:50:05.957971242 +0800
+++ third_party/xray-core/app/dispatcher/default.go	2026-08-24 20:04:23.683807491 +0800
@@ -229,6 +229,7 @@
 			}
 		}
 		link.Writer = applyBandwidthLimit(ctx, link.Writer, bandwidthManager, user)
+		link.Reader = applyBandwidthLimitReader(ctx, link.Reader, bandwidthManager, user)
 		if p.Stats.UserOnline {
 			name := "user>>>" + user.Email + ">>>online"
 			if om, _ := stats.GetOrRegisterOnlineMap(statsManager, name); om != nil {
@@ -295,9 +296,25 @@
 	if lim == nil {
 		return writer
 	}
+	DisableSpliceForBandwidthLimit(ctx)
 	return &RateLimitWriter{Writer: writer, Limiter: lim, Context: ctx}
 }
 
+// applyBandwidthLimitReader throttles the uplink half of a link. Only the
+// DispatchLink path needs it: Dispatch builds two links via getLink and already
+// covers both directions through their writers.
+func applyBandwidthLimitReader(ctx context.Context, reader buf.Reader, limiter bandwidth.Manager, user *protocol.MemoryUser) buf.Reader {
+	if limiter == nil || user == nil || user.Email == "" {
+		return reader
+	}
+	lim := limiter.GetUserLimiter(user.Email)
+	if lim == nil {
+		return reader
+	}
+	DisableSpliceForBandwidthLimit(ctx)
+	return &RateLimitReader{Reader: reader, Limiter: lim, Context: ctx}
+}
+
 // Dispatch implements routing.Dispatcher.
 func (d *DefaultDispatcher) Dispatch(ctx context.Context, destination net.Destination) (*transport.Link, error) {
 	if !destination.IsValid() {
--- /tmp/cursor-sandbox-cache/74593ecae99195a24b357adb44c35647/go-mod/github.com/cedar2025/!xray-core@v0.0.0-20260409213332-f47935539965/app/dispatcher/ratelimit.go	2026-08-24 18:50:05.957971242 +0800
+++ third_party/xray-core/app/dispatcher/ratelimit.go	2026-08-24 20:04:04.750398636 +0800
@@ -2,12 +2,40 @@
 
 import (
 	"context"
+	"time"
 
 	"github.com/xtls/xray-core/common"
 	"github.com/xtls/xray-core/common/buf"
 	"golang.org/x/time/rate"
 )
 
+// waitBytes consumes n tokens, split into chunks no larger than the bucket
+// burst. rate.Limiter.WaitN rejects any request above the burst outright, which
+// would otherwise surface as a dropped connection on large buffers.
+func waitBytes(ctx context.Context, limiter *rate.Limiter, n int) error {
+	if limiter == nil || n <= 0 {
+		return nil
+	}
+	if ctx == nil {
+		ctx = context.Background()
+	}
+	burst := limiter.Burst()
+	if burst <= 0 {
+		return nil
+	}
+	for n > 0 {
+		chunk := n
+		if chunk > burst {
+			chunk = burst
+		}
+		if err := limiter.WaitN(ctx, chunk); err != nil {
+			return err
+		}
+		n -= chunk
+	}
+	return nil
+}
+
 type RateLimitWriter struct {
 	Writer  buf.Writer
 	Limiter *rate.Limiter
@@ -15,18 +43,56 @@
 }
 
 func (w *RateLimitWriter) WriteMultiBuffer(mb buf.MultiBuffer) error {
-	if w.Limiter != nil {
-		ctx := w.Context
-		if ctx == nil {
-			ctx = context.Background()
-		}
-		if err := w.Limiter.WaitN(ctx, int(mb.Len())); err != nil {
-			buf.ReleaseMulti(mb)
-			return err
-		}
+	if err := waitBytes(w.Context, w.Limiter, int(mb.Len())); err != nil {
+		buf.ReleaseMulti(mb)
+		return err
 	}
 	return w.Writer.WriteMultiBuffer(mb)
 }
 
 func (w *RateLimitWriter) Close() error { return common.Close(w.Writer) }
 func (w *RateLimitWriter) Interrupt()   { common.Interrupt(w.Writer) }
+
+// RateLimitReader throttles the uplink direction. Writer-side limiting alone
+// only covers downlink, and inbounds that hand a full transport.Link to
+// DispatchLink (VLESS, Hysteria, mux, SOCKS, HTTP) never get their uplink
+// wrapped otherwise.
+//
+// Tokens are charged against the bytes actually read, so a large read buffer
+// that returns few bytes is not over-billed.
+type RateLimitReader struct {
+	Reader  buf.Reader
+	Limiter *rate.Limiter
+	Context context.Context
+}
+
+func (r *RateLimitReader) ReadMultiBuffer() (buf.MultiBuffer, error) {
+	mb, err := r.Reader.ReadMultiBuffer()
+	if throttleErr := r.throttle(mb); throttleErr != nil {
+		buf.ReleaseMulti(mb)
+		return nil, throttleErr
+	}
+	return mb, err
+}
+
+func (r *RateLimitReader) ReadMultiBufferTimeout(timeout time.Duration) (buf.MultiBuffer, error) {
+	tr, ok := r.Reader.(buf.TimeoutReader)
+	if !ok {
+		return r.ReadMultiBuffer()
+	}
+	mb, err := tr.ReadMultiBufferTimeout(timeout)
+	if throttleErr := r.throttle(mb); throttleErr != nil {
+		buf.ReleaseMulti(mb)
+		return nil, throttleErr
+	}
+	return mb, err
+}
+
+func (r *RateLimitReader) throttle(mb buf.MultiBuffer) error {
+	if mb.IsEmpty() {
+		return nil
+	}
+	return waitBytes(r.Context, r.Limiter, int(mb.Len()))
+}
+
+func (r *RateLimitReader) Interrupt() { common.Interrupt(r.Reader) }
--- /tmp/cursor-sandbox-cache/74593ecae99195a24b357adb44c35647/go-mod/github.com/cedar2025/!xray-core@v0.0.0-20260409213332-f47935539965/proxy/proxy.go	2026-08-24 18:50:05.993989191 +0800
+++ third_party/xray-core/proxy/proxy.go	2026-08-24 19:33:16.644640107 +0800
@@ -651,7 +651,9 @@
 				if !ok {
 					v = "Old cipher: " + strconv.FormatUint(uint64(trafficState.Cipher), 16)
 				} else if v != "TLS_AES_128_CCM_8_SHA256" {
-					trafficState.EnableXtls = true
+					// Keep Vision framing (no XTLS direct copy) so traffic is counted
+					// per-buffer like VMess WebSocket and bandwidth limits stay effective.
+					trafficState.EnableXtls = false
 				}
 				errors.LogDebug(ctx, "XtlsFilterTls found tls 1.3! ", b.Len(), " ", v)
 				trafficState.NumberOfPacketToFilter = 0
@@ -730,6 +732,10 @@
 	if inbound == nil || inbound.CanSpliceCopy == 3 {
 		return readV(ctx, reader, writer, timer, readCounter)
 	}
+	if dispatcher.UserHasBandwidthLimit(ctx) {
+		dispatcher.DisableSpliceForBandwidthLimit(ctx)
+		return readV(ctx, reader, writer, timer, readCounter)
+	}
 	outbounds := session.OutboundsFromContext(ctx)
 	if len(outbounds) == 0 {
 		return readV(ctx, reader, writer, timer, readCounter)
